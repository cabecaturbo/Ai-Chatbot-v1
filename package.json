{
  "name": "netia-chatbot-server",
  "version": "1.0.0",
  "description": "A modern chatbot server with WebSocket support",
  "main": "src/server.js",
  "scripts": {
    "dev": "cd netia-bot-safe && npm run dev",
    "check:health": "cd netia-bot-safe && npm run check:health",
    "check:webhook": "cd netia-bot-safe && npm run check:webhook",
    "test:conversations": "cd netia-bot-safe && npm run test:conversations",
    "test:load": "cd netia-bot-safe && npm run test:load",
    "prepare": "husky"
  },
  "keywords": ["chatbot", "websocket", "nodejs", "express"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "husky": "^9.1.6"
  }
}
