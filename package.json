{
  "name": "netia",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,admin \"npm --prefix apps/api run dev\" \"npm --prefix apps/admin run dev\"",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:admin": "npm --prefix apps/admin run dev",
    "build": "npm --prefix apps/api run build && npm --prefix apps/admin run build",
    "prisma:push": "npm --prefix apps/api run prisma:push",
    "start": "npm --prefix apps/api run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
